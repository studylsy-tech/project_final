<?xml version="1.0" encoding="UTF-8"?>
<!-- classpath:/mybatis/mappers/userMapper.xml -->
<!-- sql문(xml)에 명시된 결과 또는 파라미터를 java 객체로 매핑 시켜주는 역할 -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>
<mapper namespace="com.project.dao.userMapper">
	
	<resultMap id="MemberResultMap" type="com.project.model.MemberDTO">
		<result property="phone" column="PHONE" />
		<result property="pw" column="PW" />
		<result property="name" column="NAME" />
		<result property="email" column="EMAIL" />
		<result property="birth" column="BIRTH" />
		<result property="gender" column="GENDER" />
		<result property="memberType" column="MEMBER_TYPE" />
	</resultMap>
	
    <insert id="insertSemiMember" parameterType="com.project.model.MemberDTO">
        INSERT INTO MEMBERS(PHONE, PW, MEMBER_TYPE)
        VALUES(#{phone}, #{pw}, 'SEMI')
    </insert>
	
    <insert id="insertMember" parameterType="com.project.model.MemberDTO">
        INSERT INTO MEMBERS(PHONE, PW, NAME, EMAIL, BIRTH, GENDER, MEMBER_TYPE)
        VALUES(#{phone}, #{pw}, #{name}, #{email}, #{birth}, #{gender}, 'FULL')
    </insert>
	
    <select id="loginCheck" parameterType="com.project.model.MemberDTO" resultMap="MemberResultMap">
        SELECT * FROM MEMBERS WHERE PHONE = #{phone} AND PW = #{pw}
    </select>
    <select id="getBoardList" parameterType="string" resultType="com.project.model.BoardDTO">
    SELECT * FROM BOARD 
    WHERE BOARD_TYPE = #{board_type} 
    ORDER BY NOTICE_NO DESC
</select>

<insert id="insertBoard" parameterType="com.project.model.BoardDTO">
    INSERT INTO BOARD (TITLE, CONTENT, WRITER, BOARD_TYPE)
    VALUES (#{title}, #{content}, #{writer}, #{board_type})
</insert>
</mapper>